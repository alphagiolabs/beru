.DS_Store
node_modules
/build
/dist-electron
/dist-installer
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# PowerShell accidental redirection artifact
$null

# Python cache
__pycache__/
.pytest_cache/
*.pyc
python/benchmark_*.py

# Local test media / benchmark output (never commit)
prueba.mp4
prueba.*
/*.mp4
/*.mov
/*.mkv
tmp/

# FFmpeg binaries (large, generated by npm postinstall)
bin/ffmpeg.exe
bin/ffprobe.exe
bin/ffmpeg.exe.LICENSE
bin/ffprobe.exe.LICENSE

# Local AI tooling / agent config (not part of the project)
.agents/
.commandcode/
skills-lock.json
AGENTS.md

# Versioned build output directories (never commit)
dist-installer-v*/
dist-build/
